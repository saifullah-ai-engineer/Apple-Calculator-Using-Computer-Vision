{
  "name": "mathcam-flask-app",
  "private": true,
  "version": "1.0.0",
  "description": "Flask, OpenCV, and Gemini-powered hand gesture math solver.",
  "scripts": {
    "build": "echo 'No frontend build step required; Flask serves templates and static assets.'",
    "start": "python app.py"
  }
}
